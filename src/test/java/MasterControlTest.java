public class MasterControlTest {
/*
    MasterControl masterControl;

    @BeforeEach
    void setUp() {
        Bank bank = new Bank();
        CreateCommandValidator createCommandValidator = new CreateCommandValidator(bank);
        DepositCommandValidator depositCommandValidator = new DepositCommandValidator(bank);
        masterControl = new MasterControl(bank, new CreateCommandValidator(bank), new DepositCommandValidator(bank), new CreateCommandProcessor(bank), new DepositCommandProcessor(bank), new CommandStorage(createCommandValidator, depositCommandValidator));
    }

    @Test
    void typo_in_create_command_is_invalid() {
        List<String> input = new ArrayList<>();
        input.add("creat checking 12345678 1.0");

        List<String> actual = masterControl.start(input);
        assertEquals(1, actual.size());
        assertEquals("creat checking 12345678 1.0", actual.get(0));
    }

 */
}
